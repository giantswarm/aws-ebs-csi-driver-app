appName: "aws-ebs-csi-driver" # Needs to match the name in the release
repoName: "aws-ebs-csi-driver-app"
appCatalog: "default"

providers:
  - capa

helmReleaseSourceRef:
  name: giantswarm-test-catalog
